{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main DaggerCraft Toolbox window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
